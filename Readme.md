1. clone the repository
2. create and activate a virtual environment
3. install dependencies run `pip install -r requirements.txt`
4. run the script run `python app.py`
5. check the output in the browser
